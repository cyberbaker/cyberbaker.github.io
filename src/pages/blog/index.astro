---
import PostListing from '../../components/PostListing.astro';
import { getPublishedPosts } from '../../posts';

const posts = await getPublishedPosts();
---

<PostListing
	title="All posts"
	lead="Every post, newest first: plain-English explainers and technical deep dives."
	posts={posts}
	current="all"
/>
